//iv snippit
import { it, expect, describe } from 'vitest'

//d+enter to create test suite
describe('group', () => {
    //i+enter to create test case
    it('should', () => {
        expect(1).toBeTruthy();
    })
})